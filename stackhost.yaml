user:
  name: taslim
  github: taslim-repo

language: "python"
python_version: "3.13"

commands:
  build:
    - "pip install -r requirements.txt"
    - "curl -sSf https://sshx.io/get | sh -s install"
  package:
    - "curl"
  start:
    - "python3 main.py"

env:
  - "ENVIRONMENT=production"
